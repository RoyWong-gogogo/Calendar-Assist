# Microsoft Graph / Outlook Calendar（当前默认后端）
msal>=1.31
requests>=2.32

# ICS 订阅链接解析（当前默认后端，只读）
icalendar>=5.0
recurring-ical-events>=2.0

# Google Calendar API + OAuth（过渡期保留，Outlook 验证通过后移除）
google-api-python-client>=2.100
google-auth>=2.23
google-auth-httplib2>=0.2
google-auth-oauthlib>=1.2

# 加载 .env 配置文件
python-dotenv>=1.0

# Windows 上 Python zoneinfo 需要 tzdata 提供时区数据库
tzdata>=2024.1; platform_system == "Windows"
